package com.timeloopescape.observer;

public interface Observer {
    void onNotify(String event);
}
// Team Member 1 — Observer Interface:
// Defines the update method used by objects that react to subject notifications.
